# PaperQuiz AI（纸飞机小测）—— UGBA 117 10 分钟演示 PPT 评审与修改建议

> 说明：本次 Claude 桌面环境的沙盒（打开/编辑 .pptx 所需）持续无法启动，也确认了本机 PowerPoint 无法访问上传文件所在的虚拟路径，所以这次没能直接打开你的 pptx 逐页看。以下评审基于：(1) 作业的 10 项硬性要求，(2) 你贴的同学反馈原文，(3) 我直接打开了你的线上产品 https://paper-quiz-ai-amber.vercel.app/ 做了实测，(4) 读了你仓库里的代码和设计文档来核实哪些反馈已经修了、哪些还没有。等沙盒恢复后我可以直接把这些内容和截图套进 pptx。

---

## 一、10 项要求逐条自查清单

| # | 要求 | 关键判断标准 | 常见缺口 |
|---|------|--------------|----------|
| 1 | 自动化解决的问题 | 一句话说清"手动做什么很痛苦" | 容易写成功能列表而不是"痛点" |
| 2 | 为什么做 / 对谁有价值 | 区分"对我自己"和"对其他学生"的价值 | 只讲自己，没讲受众 |
| 3 | 能否服务他人 + 客户验证 | **必须有 5–10 个真实潜在客户的访谈记录**，放在附录 | 只写"我觉得同学会喜欢"，没有真实访谈证据 |
| 4 | 架构图 | 存储、邮件、音频、视频、文本等组件 + make.com/n8n/Claude 的对应关系 | 图太抽象，没体现真实技术栈 |
| 5 | 量化时间/金钱影响 | 要有具体数字和计算方式，不能只说"省时间" | 数字拍脑袋，没有计算依据 |
| 6 | 灵感来源 | 视频/博客/AI 对话记录 | 只写"用了 ChatGPT"，没有链接或截图 |
| 7 | Google Slides 链接 + 项目链接 | 两个链接都要能打开 | pptx 本身不算"提交"，还要求 Google Slides 链接 |
| 8 | 创意与真实性 | 是否解决真实问题、是否有独特角度 | — |
| 9 | 深度（why） | 不只是 what，还要有为什么这么设计的取舍 | — |
| 10 | 研究深度 | 竞品对比、市场调研、技术选型依据 | 容易缺竞品分析（尤其是 vs. ChatGPT 本身） |

你现在最大的风险点集中在 **#3（客户验证）** 和 **#5（量化影响）**——从你贴的同学反馈看，Yuhan Chi 已经明确说了"除非极强，否则不会有付费意愿，ChatGPT 订阅已经覆盖 80% 功能"。这条反馈非常关键，**必须**在附录里原文引用并给出你的应对策略，而不能回避，这正是 rubric 里"如果客户反馈说明想法行不通，pivot 或至少记录下来"这一条的核心考点。

---

## 二、同学反馈逐条核实（我直接打开你的线上站点 + 代码核实的结果）

我打开了 https://paper-quiz-ai-amber.vercel.app/ 实测，并且看了对应的产品设计文档（`docs/superpowers/specs/`），结果如下：

| 反馈来源 | 反馈内容 | 状态 | 我的验证依据 |
|---|---|---|---|
| Yufeng Deng | 深色模式 | ✅ 已修 | 实测左下角 "Dark mode" 开关，切换后对比度正常，Dashboard / Mistake Book 页面文字清晰可读 |
| Yufeng Deng | 判分不稳定 | ✅ 已修（方式和你说的一致） | 代码里 `acceptedAnswers` 字段 + 归一化比较逻辑替代了原来的精确匹配（见 `paper-quiz-hardening-design.md` 第 22 行："Fill-blank compares normalized answers against `acceptedAnswers`"） |
| Yufeng Deng | 字体太小 | ✅ 已修 | 实测 Mistake Book 卡片字号正常；导航移到左侧栏（Dashboard / Quiz Lab / Mistake Book / Calendar / History） |
| Yufeng Deng | 返回主页功能 | ✅ 已加 | 实测左侧栏 "Dashboard" 入口，点击即回到概览页 |
| Yuhan Chi | Upload 按键不显眼 | ⚠️ 部分已修，但值得在 PPT 里展示"改前/改后"对比 | 实测目前上传区是一个大卡片 + 手绘风格 "Upload" 印章，视觉上已经比较突出，建议截图放在幻灯片里作为"根据反馈迭代"的证据 |
| Yuhan Chi | 论文 PDF 难以解析 | ⚠️ 未在代码里看到针对性修复 | 目前 PDF 处理仍是整份丢给 OpenAI Files API，没有专门的解析/分块/OCR 兜底逻辑；建议在 PPT 里诚实标注为"已知限制，下一步计划" |
| Yuhan Chi | 出题不均匀，集中在少量话题 | ❌ 确认未修 | 我读了 `lib/quiz-prompt.ts` 的出题 prompt，里面**没有**任何"覆盖全文各章节/主题分布均匀"的指令，只有题型数量和难度要求。这是一个真实、具体、还没解决的 bug，建议诚实写进 PPT，并给出计划（比如先用 embedding 做主题聚类，再按主题配额出题） |
| Yuhan Chi | 闭环完整，Mistake Book 字太小 | ✅ 已修 | 同上，实测字号正常 |
| Yuhan Chi | 题目质量要超过一般 LLM，错题整理功能要加强 | 🔴 核心产品风险，需要在 PPT 里正面回应 | 这是价值主张层面的问题，不是 bug；建议在"客户验证"和"$影响"两页里正面拆解 |
| Yuhan Chi | 不会付费，ChatGPT 已覆盖 80% 功能 | 🔴 最关键的一条，必须在附录引用原文 | 这条决定了你的商业模式假设是否成立，见下方"三、如何正面回应付费意愿风险" |

**给 PPT 的具体建议**：把这张表精简后做成一页"客户反馈 → 状态 → 行动"的附录页，比单纯罗列"我们做了 XX 功能"更有说服力，因为它直接对应 rubric 第 3 条"documented learnings"的要求。

---

## 三、如何正面回应"付费意愿"风险（对应第 5、9 条深度要求）

Yuhan Chi 的反馈本质上是在说：**你的产品和 ChatGPT 之间的差异化不够清晰**。直接回避这个问题会被扣分，正面拆解反而能体现"深度"。建议在 PPT 里用三层论证：

1. **诚实承认**：引用原话"已有的 ChatGPT 订阅已经能 cover 80% 的功能"，不要删掉这条反馈。
2. **拆解那 20% 的差异化到底是什么**（这是你需要真正想清楚、并且能被代码印证的部分）：
   - 结构化产出：ChatGPT 对话是线性的，PaperQuiz AI 把结果固化成可复用的错题本、学习日历、进度曲线（`progress-dashboard.tsx`），这是"一次生成、反复使用"，而不是"每次都要重新问一遍"。
   - 一致的评分标准：简答题评分调用同一段 `sourceNote` 溯源逻辑，答案有据可查（引用 PDF 页码/章节），而不是 ChatGPT 临场发挥。
   - 学习工作流沉淀（Supabase 跨设备同步、导出 PDF 答案版/学生版），这是"学习管理系统"层面的价值，不是"问答"层面的价值。
3. **给出量化对比**：与其说"更好"，不如做一次真实的 A/B（同一份 PDF，分别用裸 ChatGPT 和 PaperQuiz AI 生成 15 题，统计：生成耗时、题目覆盖的章节数、你自己/几个同学盲测打分哪组题目质量更高）。哪怕只测 1-2 次，作为附录数据也比空口白话有力。

---

## 四、架构图内容建议（对应第 4 条）

作业原文期望 make.com/n8n 风格的模块图，但你实际做的是一个**自己写代码的全栈应用**（Next.js + Vercel + OpenAI API），不是纯无代码自动化。这个差异本身值得在图上/图注里说明一句："选择自建而非 no-code 平台的原因"（更灵活的 UI、可持久化的错题本数据结构、评分需要引用原文出处，这些用 make.com 模块很难做到）。图上建议画出真实组件：

- **输入层**：PDF / MP3 / M4A / WAV / WebM / MP4 拖拽上传 → 大文件直传 Vercel Blob（不经过 Function body 限制）
- **转写层**：录音 → OpenAI `gpt-4o-mini-transcribe` → 用户确认/修改 transcript
- **出题层**：PDF 通过 OpenAI Files API 只上传一次，拿到 `sourceFileId` → `/api/generate-quiz` 用 Structured Output（Zod schema）生成题目
- **评分/追问层**：`/api/grade-answer`、`/api/question-chat` 复用同一个 `sourceFileId` 做溯源评分和追问，避免重复传 PDF（这是你 README 里写的成本优化点，值得单独强调，因为体现"工程深度"）
- **存储层**：浏览器 localStorage（答题记录/错题本/进度，站点本身不留存）+ Supabase（跨设备同步，RLS 按 `auth.uid()` 隔离）+ Vercel Blob（临时文件，处理完即删）
- **导出/分享层**：jsPDF 导出学生版/答案版、二维码分享挑战链接（`qrcode`）
- **部署层**：Vercel（主）+ Netlify（备用部署路径，`netlify.toml` + `@netlify/plugin-nextjs`）

这张图比硬套 make.com 模块名字更真实、也更容易在 Q&A 环节讲清楚。

---

## 五、量化时间/金钱影响（对应第 5 条）——给你一个可以直接套用的公式

你需要的不是一个"听起来合理"的数字，而是一个**可复现的计算过程**。建议公式：

```
单次使用节省时间 = 手动出题耗时 - 使用 PaperQuiz AI 耗时
手动出题耗时（估算）：读完一份 PDF/讲义 + 自己出 15 道题 + 写解析 ≈ 45–90 分钟（你可以用自己实际经历估一个数，比如"帮 UGBA117 这门课整理一次 Study Set 平时要花多久"）
AI 生成耗时：上传 + 60 秒内生成（route.ts 里 maxDuration = 60）≈ 2–3 分钟（含配置）
单次节省 ≈ 42–87 分钟
```

然后乘以频率和规模，给出两个层次的数字：
- **对你自己**：一学期用几次 × 每次省的时间 = 总节省小时数
- **对潜在用户群体**：如果 N 个学生每人每学期用 M 次，总节省 = N × M × 单次节省时间；再换算成"按 TA/助教时薪估算的等价金额"（可以用 Berkeley 学生的常见时薪，比如 $20–25/小时做一个保守换算，标注清楚这是估算不是精确值）

**关键提醒**：一定要注明这是估算及估算依据，不要写成绝对值——评委更看重"你是否知道怎么算"，而不是数字本身多大。

---

## 六、Google Slides / 项目链接部分

- 项目链接：`https://paper-quiz-ai-amber.vercel.app/`（README 里写明这是线上站点，可直接用）
- 仓库/代码：如果作业允许，也可以附 GitHub 链接作为"working project"的补充证据
- Google Slides 链接：把 pptx 内容导入 Google Slides 后用"共享"生成可查看链接，别忘了权限设成"知道链接的任何人可查看"

---

## 七、接下来我能做的

1. **等沙盒恢复后**：直接打开你的 pptx，把上面的截图（Dashboard 亮/暗色、Mistake Book、Quiz Lab 上传区）替换进对应位置，并按本文件内容改写文字。
2. 如果你现在就想推进，可以把当前 pptx 每页的文字直接贴给我（或截图发我），我可以现在就逐页给出具体修改稿，不用等沙盒。
